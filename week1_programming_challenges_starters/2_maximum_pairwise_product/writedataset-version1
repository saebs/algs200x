package main

import (
	"fmt"
	"math/rand"
	//	"os"
)

func DatasetWrite(limit int) []int {
	dataset := make([]int, limit)
	var data int = 0
	for i := 0; i < limit; i++ {
		data = rand.Intn(limit)
		dataset[i] = data
	}
	dataset2 := rand.Perm(limit)
	fmt.Println(dataset2)
	return dataset2
}
